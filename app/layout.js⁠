import "./globals.css";

export const metadata = {
  title: "Hi-Fi Horizon",
  description: "Modern glassmorphism music player",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
